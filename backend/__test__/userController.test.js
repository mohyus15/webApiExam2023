const request = require('supertest');
const app = require('../app.js');
describe('Test api for login', () => {
	it('It should respond with 200 created login', () => {
		console.log('testing');
	});
});
